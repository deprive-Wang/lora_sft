# LoRA/QLoRA SFT 实验报告

日期：2026-08-22
基座：Qwen/Qwen2.5-1.5B-Instruct　|　数据：ultrachat_200k 单轮子集（9,500 train / 500 val）
训练平台：AutoDL RTX 3090　|　本地验证：RTX 3070 Laptop 8GB

---

## 1. 项目背景与目标

在 1.5B 的指令模型上做一次小规模 SFT，完整走通并理解三个核心环节：

1. **label mask**——决定"学什么"：SFT 的 loss 只应来自 assistant 回复段；
2. **LoRA**——决定"用多少参数学"：冻结基座，只训练低秩增量；
3. **QLoRA**——决定"冻结的部分用多少显存放"：基座压到 4-bit NF4。

全量微调一个 1.5B 模型，AdamW 优化器状态 + 梯度约为权重的 4 倍显存开销，FP16 权重约 3 GB，全量训练对消费级显卡压力很大。LoRA 把"可训练参数"砍到 0.3%，QLoRA 再把"冻结权重"的存储压到 4-bit（约 1.3 GB），让 24 GB 的 3090 轻松完成、8 GB 的 3070 也能推理。

## 2. 核心原理速览（复习要点）

### 2.1 LoRA：低秩增量

假设微调引起的权重变化 ΔW 是低秩的，可分解为两个小矩阵。对线性层 W（d×k）：

```
h = W·x + (α/r)·B·A·x      A: r×k，B: d×r，r ≪ d,k
```

- W 冻结；只训练 A、B。B 初始为 0、A 随机，起点时 ΔW = 0，模型行为与基座一致。
- 本项目参数：r=16、α=32（缩放 α/r = 2）、dropout 0.05、目标模块 q/k/v/o（注意力的四个投影）、bias 不训练。
- 参数量账：q/o_proj 是 1536×1536，k/v_proj 因 GQA（12 Query 头 / 2 KV 头）是 1536×256。每个矩阵 LoRA 参数 = r·(d+k)：
  - q/o：16 × (1536+1536) = 49,152；k/v：16 × (1536+256) = 28,672
  - 每层 155,648，28 层共约 436 万，占 1.5B 基座的 **0.3%**
  - 实测 adapter 文件 17,462,432 字节 = 4,365,608 参数 × 4 字节（fp32 保存），与推算吻合
- adapter 存的是 A、B 分解形式（几 MB），不是 ΔW 本身（那会是 d×k 的大矩阵）；ΔW 是使用时展开或合并后的产物。
- LoRA 学习率量级 2e-4，比全量微调（1e-5 级）大一个数量级：更新的只是低秩增量，扰动影响面小，可用大步长。

### 2.2 QLoRA：4-bit 存储 + FP16 计算

三个关键设计对应三个配置：

| 设计 | 配置 | 作用 |
|---|---|---|
| NF4 量化 | `bnb_4bit_quant_type="nf4"` | 按正态分位点设计 16 个格点，权重密集区格点更密，比均匀 FP4 信息损失小 |
| 双重量化 | `bnb_4bit_use_double_quant=True` | 量化常数本身再量化，每参数再省约 0.4 bit |
| 计算时反量化 | `bnb_4bit_compute_dtype=fp16` | 存储 4-bit，矩阵乘时逐层临时还原 FP16 计算 |

注意：**adapter（A、B）全程不量化**，始终以高精度训练；4-bit 的只有冻结基座。

配套的两个必要操作：
- `prepare_model_for_kbit_training`：LayerNorm 等敏感层升回 FP32，并准备 gradient checkpointing（用反向重算换激活显存）；
- `use_cache = False`：推理用 KV cache 与 checkpointing 的重算机制冲突，训练时必须关。

### 2.3 label mask：两次完整编码 + 前缀校验

SFT 的学习目标是"给定指令生成回复"，prompt 段不该产生梯度。实现（dataset.py）：

```
prompt_ids = chat_template([user], add_generation_prompt=True)   # 编码到 <|im_start|>assistant\n
full_ids   = chat_template([user, assistant])                    # 前缀 + 回复正文 + <|im_end|>
labels     = [-100] × len(prompt_ids) + full_ids[len(prompt_ids):]
```

- -100 是 CrossEntropyLoss(ignore_index=-100) 的约定，被 mask 位置不产生梯度。
- **为什么不能手动拼字符串或分段编码**：BPE 存在跨边界合并，分段编码再拼接与整段编码的 token 序列不一定相同，mask 边界会与 token 边界错位。所以用两次完整编码，再校验 prompt_ids 是 full_ids 的严格前缀，对齐才有保证（每条样本都校验，不符即抛错）。
- train.py 的 validate_sample 逐条复检契约（长度一致、非 -100 的 label 必须等于对应 input_id、loss 段非空），训练侧不重复分词。

## 3. 项目结构与数据流

```
ultrachat_200k (HuggingFace streaming)
  └─ scripts/prepare_data.py      抽取首轮单轮样本 → data/ultrachat_single_turn_raw.jsonl
      └─ dataset.py               chat template 编码 + label mask + 自检 → data/sft_train.jsonl / sft_val.jsonl
          └─ train.py             校验 JSONL、动态右 padding、4-bit 基座 + LoRA 训练
              ├─ checkpoints/qwen2_5_1_5b_ultrachat_lora/   adapter + 指标（含 checkpoint-297/594）
              └─ tf-logs/                                    TensorBoard 事件（自云端 /root/tf-logs 拉回）
                  ├─ inference.py    adapter 开/关推理对比（本地）
                  └─ eval_baseline.py  adapter 开/关 eval loss 对比（本地）
```

- dataset.py 不依赖 torch，编码与自检可在无 GPU 环境完成；训练输入必须是 token id JSONL，train.py 不重新套模板/分词/重建 labels。
- max_len=1024，超长直接丢弃（不截断）；有效样本目标 9,500/500。
- 动态右 padding：batch 内取最长，pad 位置 attention_mask=0、label=-100。

## 4. 训练配置

| 项 | 值 | 备注 |
|---|---|---|
| 量化 | 4-bit NF4 + double quant | 计算 dtype fp16 |
| LoRA | r=16, α=32, dropout=0.05 | 目标 q/k/v/o，bias none |
| batch | micro 2 × 梯度累积 16 | 有效 batch 32 |
| 学习率 | 2e-4，线性衰减到 0 | LoRA 量级 |
| 精度 / epochs | fp16 / 2 | gradient checkpointing 开启 |
| 评估 / 保存 | 每 epoch eval / 每 epoch 存 | save_total_limit=2 |
| 步数 | 297/epoch × 2 = 594 | 9500 ÷ 32 = 296.9 |
| 耗时 | 4,016 s ≈ 67 min | 3090 |
| 显存 | 4-bit 基座约 1.3 GB + 激活 | 24 GB 卡余量极大 |

## 5. 实验结果

### 5.1 训练过程

| 指标 | 数值 |
|---|---|
| step 1 train loss | 1.280 |
| 全程 train loss 均值 | 1.143 |
| eval loss（epoch 1 → 2） | 1.0914 → 1.0887 |
| 验证集 perplexity（末值） | e^1.089 ≈ 2.97 |
| grad_norm 范围 | 0.13–0.19，平稳无尖峰 |

曲线形态：前 30 步快速下降（1.28→1.16，B 从零起步的最有效学习期）→ 1.10–1.18 噪声平台 → 第二 epoch 仅再降 0.0027，**已饱和但无过拟合**（eval 未回升，最终取 epoch 2 模型正确）。

两个解读要点：
- **eval loss (1.089) < train loss (1.143)**：不是泄漏，是 lora_dropout 只在训练生效 + train loss 含早期较差阶段的滑动平均。eval < train 通常是正则化正常工作的健康信号。
- **第二 epoch 无收益**：容量（r=16）与数据（9.5k 通用对话）在此任务上已榨干；改进方向是加 r 或扩目标模块，而不是加 epoch。

### 5.2 baseline 对比（同硬件同口径，本地 3070）

| 配置 | eval loss | perplexity |
|---|---|---|
| BASE（adapter 关闭） | 1.1456 | 3.14 |
| SFT（adapter 开启） | 1.0886 | 2.97 |
| 净收益 | **-0.0570（约 -5%）** | -5.5% |

- **跨硬件复现**：本地 SFT eval 1.0886 vs 云端 3090 的 1.0887，仅末位小数差异（浮点归约顺序），链路可信。
- **基座起点低是关键背景**：Qwen2.5-1.5B-**Instruct** 本身已指令微调过，在通用对话数据上起点已高（1.1456），可学空间小——这同时解释了 loss 快速饱和和推理端"风格迁移为主"的现象。

### 5.3 推理行为对比（贪心解码，差异全部来自 ΔW）

三条测试 prompt 的 BASE vs SFT 对比结论：

1. **指令遵循更好**：解释"过拟合"时，BASE 答完定义又补充解决方法（超出指令范围），SFT 严格停留在定义。
2. **风格向训练数据迁移（最清晰的指纹）**：BASE 用 markdown 加粗标题，SFT 变成纯文本编号列表——UltraChat 回复以朴素对话体为主。
3. **知识准确性持平甚至略降**：天空为什么蓝一题，SFT 版出现事实性错误（"leaving only red and orange"是晚霞逻辑）。9.5k 通用对话 + 1.5B 模型学到的是**分布与风格迁移，而非知识注入**。
4. 无乱码、无模板标记泄漏、无灾难性遗忘。

### 5.4 综合解读

三组证据互相闭环：loss 净收益小（0.057）↔ 训练曲线快速饱和 ↔ 推理端行为差异是风格性的。QLoRA 论文的核心结论（NF4 基座 + LoRA 可追平 FP16 基座 + LoRA）在本实验尺度下得到一致性验证。

## 6. 疑难点与解决方法

### 6.1 数据与编码

| 问题 | 原因 | 解决 |
|---|---|---|
| mask 边界与 token 边界错位风险 | BPE 跨边界合并，分段编码拼接 ≠ 整段编码 | 两次完整编码 + 严格前缀校验，不符即抛错 |
| 整条样本无梯度 | 极端情况 loss 段为空 | validate_sample 校验"至少有一个非 -100 label" |
| transformers v4/v5 apply_chat_template 返回类型不同 | v5 返回 BatchEncoding 而非 list | dataset.py 的 template_ids 统一收敛为 list[int] |

### 6.2 transformers 5.x 兼容

| 问题 | 解决 |
|---|---|
| TrainingArguments 移除 logging_dir，5.x 构造时抛 TypeError | 按构造签名条件传参（与 eval_strategy 兼容写法一致）+ 5.x 走 `TENSORBOARD_LOGGING_DIR` 环境变量 |
| eval_strategy 改名 | 同样按签名探测 |
| torch_dtype 改名 dtype | 实测 5.x 仅 deprecation 警告仍可用，暂不改以兼容 4.x |

### 6.3 AutoDL 部署

| 现象 | 根因 | 解决 |
|---|---|---|
| pip 报 "from versions: none" 且无 Retrying 告警 | **无卡模式开机：无 GPU 也无外网** | 诊断树先跑 `nvidia-smi`；无卡模式只能做内网文件上传（数据盘 /root/autodl-tmp 跨模式持久），换模式必须关机再开机 |
| GPU 模式下 pip 同样报错，curl 镜像返回 403 | network_turbo 残留代理被阿里云拒，或阿里云封机房出口 IP | `unset http_proxy https_proxy` 后复测；仍 403 则 `pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple` 永久换清华源 |
| 控制台 TensorBoard 无数据 | AutoDL TB 固定读 /root/tf-logs，而训练默认写 experiments/ | `python train.py --logging-dir /root/tf-logs`（脚本本就有该参数，无需改代码）；已写入的日志可软链过去 |

pip 诊断树（按序）：`nvidia-smi`（报错=无卡模式）→ `curl -sI https://mirrors.aliyun.com/pypi/simple/transformers/`（403=代理或封 IP）→ `python --version`（≤3.8=镜像 Python 太老需新建 3.12 环境）。规律：**pip 前别开 network_turbo**，它只用于 GitHub clone 和 HF 直连。

### 6.4 本地下载与推理环境

| 现象 | 根因 | 解决 |
|---|---|---|
| from_pretrained 报 "does not appear to have model.safetensors" | hf-mirror 对 LFS 权重 308 重定向回官方站，transformers 5 解析失败 | 本机可直连 huggingface.co，本地下载不用镜像 |
| 下载反复停滞（走停式限速） | huggingface_hub 1.28 默认 xet 后端不稳定 | `HF_HUB_DISABLE_XET=1` 走经典 HTTP，速度稳定 |
| 找不到已下载的 tokenizer | 本机 `HF_HOME=D:\AI_model\huggingface`，缓存不在 C 盘默认位置 | 环境探测以 huggingface_hub.constants.HF_HUB_CACHE 为准 |
| 推理/评估受网络抖动影响 | from_pretrained 默认会尝试联网校验 | 运行时加 `HF_HUB_OFFLINE=1` 强制走本地缓存 |
| 担心 Windows 本地跑不了 4-bit | — | 实测 bitsandbytes 0.50.1 + torch 2.5.1+cu121 在 Windows 3070 8GB 正常（加载、生成、eval 全通过） |

### 6.5 训练侧其他要点

- KV cache 与 gradient checkpointing 冲突 → `use_cache=False`；
- fp16 全程无 NaN，grad_norm 远低于 clip 阈值 1.0，未触发裁剪；
- 断点续训依赖 checkpoint-*/optimizer.pt（各 35 MB），确认不续训可删，最终 adapter 以输出目录根那份为准。

## 7. 复盘与后续实验方向

1. **base 模型对照**（最有价值）：把 MODEL_ID 换成 `Qwen/Qwen2.5-1.5B`（非 Instruct）重训，预期 loss 降幅远大于 0.057——那才是 SFT 威力的完整展示，也能量化"Instruct 起点"的影响。
2. **容量实验**：r 提到 32/64，或 target_modules 扩到 FFN 的 gate/up/down_proj，验证"第二 epoch 饱和是容量瓶颈"的判断。
3. **部署实验**：`merge_and_unload()` 把 ΔW 合并进基座存单文件，对比外挂 adapter 两种形态。
4. 评估口径提醒：跨 GPU 对比时同硬件跑两个数（adapter 开/关），不混用不同机器的数值。

## 8. 产物与环境速查

| 路径 | 内容 |
|---|---|
| `checkpoints/qwen2_5_1_5b_ultrachat_lora/` | 最终 adapter（17.5 MB）、trainer_state.json（完整 log history）、train/eval 指标、tokenizer；内含 checkpoint-297/594 |
| `tf-logs/` | TensorBoard 事件（本地 `tensorboard --logdir tf-logs` 可回看曲线） |
| `data/sft_train.jsonl` / `sft_val.jsonl` | token id 级训练数据（9500/500） |
| 本地解释器 | `E:\miniconda\envs\lora_sft\python.exe`（torch 2.5.1+cu121 / transformers 5.15 / peft 0.20 / bnb 0.50.1） |
| 基座缓存 | `D:\AI_model\huggingface\hub\models--Qwen--Qwen2.5-1.5B-Instruct`（3.09 GB） |
| 云端仓库 | `/root/autodl-tmp/upload_package/lora_sft`（AutoDL 3090） |
| 本地复现命令 | `HF_HUB_OFFLINE=1 python inference.py`（推理对比）；`HF_HUB_OFFLINE=1 python eval_baseline.py`（loss 对比） |
